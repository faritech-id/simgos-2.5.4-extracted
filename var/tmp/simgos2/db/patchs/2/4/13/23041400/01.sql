USE `master`;

ALTER TABLE `referensi`
	ENGINE=InnoDB,
	ADD COLUMN `TABEL_ID` INT NOT NULL AUTO_INCREMENT FIRST,
	CHANGE COLUMN `ID` `ID` SMALLINT NOT NULL AFTER `JENIS`,
	DROP PRIMARY KEY,
	ADD PRIMARY KEY (`TABEL_ID`),
	ADD UNIQUE INDEX `JENIS_ID` (`JENIS`, `ID`);
