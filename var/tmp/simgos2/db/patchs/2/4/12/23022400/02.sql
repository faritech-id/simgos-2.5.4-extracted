USE pembayaran;

ALTER TABLE `pembayaran_tagihan`
	CHANGE COLUMN `TANGGAL_DIBUAT` `TANGGAL_DIBUAT` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP AFTER `TRANSAKSI_KASIR_NOMOR`,
	ADD COLUMN `DIUBAH_TANGGAL` TIMESTAMP NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP AFTER `OLEH`,
	ADD COLUMN `DIUBAH_OLEH` MEDIUMINT NULL DEFAULT NULL AFTER `DIUBAH_TANGGAL`,
    ADD INDEX `STATUS` (`STATUS`);