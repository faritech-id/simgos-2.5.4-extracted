use `medicalrecord`;

ALTER TABLE `pemeriksaan_emg`
	ADD COLUMN `DOKTER` SMALLINT(5) NOT NULL DEFAULT 0 AFTER `IMPRESSION`,
	CHANGE COLUMN `DIBUAT_TANGGAL` `DIBUAT_TANGGAL` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP AFTER `DOKTER`;
