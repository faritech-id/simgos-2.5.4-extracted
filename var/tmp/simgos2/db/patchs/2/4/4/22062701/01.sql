USE pembayaran;

ALTER TABLE `penjamin_tagihan`
	ADD COLUMN `MANUAL` TINYINT NOT NULL DEFAULT '0' COMMENT 'Jika di edit manual di from tagihan maka menjadi 1' AFTER `PERSENTASE_TARIF_INACBG_KELAS1`,
	ADD COLUMN `OLEH` SMALLINT NOT NULL AFTER `MANUAL`,
	ADD COLUMN `TANGGAL` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP AFTER `OLEH`;
