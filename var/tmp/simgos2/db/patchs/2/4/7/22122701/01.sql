USE pembayaran;

ALTER TABLE `tagihan_pendaftaran`
	ADD COLUMN `ID` BIGINT NOT NULL AUTO_INCREMENT FIRST,
	ADD COLUMN `REF` CHAR(19) NOT NULL DEFAULT '' AFTER `PENDAFTARAN`,
	DROP PRIMARY KEY,
	ADD INDEX `REF` (`REF`),
	ADD PRIMARY KEY (`ID`),
	ADD UNIQUE INDEX `TAGIHAN_PENDAFTARAN_REF` (`TAGIHAN`, `PENDAFTARAN`, `REF`);
