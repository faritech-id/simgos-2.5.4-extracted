USE pembayaran;

ALTER TABLE `tagihan_klaim`
	CHANGE COLUMN `STATUS` `STATUS` TINYINT NOT NULL DEFAULT '0' COMMENT '0=Belum Klaim; 1=Proses Verifikasi; 2=Pending; 3=Klaim' AFTER `TOTAL`,
	ADD COLUMN `KLAIM_TANGGAL` DATETIME NULL AFTER `STATUS`,
	ADD COLUMN `KLAIM_OLEH` SMALLINT NULL AFTER `KLAIM_TANGGAL`,
	ADD COLUMN `KLAIM_CATATAN` VARCHAR(500) NULL DEFAULT NULL AFTER `KLAIM_OLEH`;
