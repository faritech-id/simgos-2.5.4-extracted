USE pendaftaran;

ALTER TABLE `penjamin`
	ADD COLUMN `NAIK_KELAS` TINYINT NULL COMMENT '1 Kelas 3 2 Kelas 2 3 Kelas 1' AFTER `CATATAN`,
	ADD COLUMN `PEMBIAYAAN` CHAR(1) NULL DEFAULT '' COMMENT 'sesuai dgn referensi penjamin' AFTER `NAIK_KELAS`,
	ADD COLUMN `PENANGGUNGJAWAB` VARCHAR(100) NULL DEFAULT NULL AFTER `PEMBIAYAAN`,
	ADD COLUMN `TUJUAN_KUNJUNGAN` CHAR(1) NULL DEFAULT '0' COMMENT 'sesuai dgn referensi penjamin' AFTER `PENANGGUNGJAWAB`,
	ADD COLUMN `PROCEDURE` CHAR(1) NULL DEFAULT '' COMMENT 'sesuai dgn referensi penjamin' AFTER `TUJUAN_KUNJUNGAN`,
	ADD COLUMN `PENUNJANG` CHAR(2) NULL DEFAULT '' AFTER `PROCEDURE`;
