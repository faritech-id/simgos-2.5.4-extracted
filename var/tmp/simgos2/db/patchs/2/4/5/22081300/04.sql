USE generator;

ALTER TABLE `id_pengguna_akses_log`
	ADD COLUMN `ID` BIGINT NOT NULL AUTO_INCREMENT FIRST,
	CHANGE COLUMN `NOMOR` `NOMOR` INT(10) NOT NULL AFTER `TANGGAL`,
	DROP PRIMARY KEY,
	ADD PRIMARY KEY (`ID`),
	ADD UNIQUE INDEX `TANGGAL_NOMOR` (`TANGGAL`, `NOMOR`);
