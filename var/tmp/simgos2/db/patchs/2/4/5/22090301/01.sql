USE aplikasi;

ALTER TABLE `petunjuk`
	CHANGE COLUMN `ID` `ID` CHAR(10) NOT NULL FIRST,
	ADD COLUMN `LEVEL` TINYINT NOT NULL DEFAULT 1 AFTER `ID`,
	ADD COLUMN `JUDUL` VARCHAR(150) NOT NULL AFTER `LEVEL`,
	CHANGE COLUMN `DESKRIPSI` `DESKRIPSI` VARCHAR(500) NOT NULL AFTER `JUDUL`,
	ADD COLUMN `URL` VARCHAR(500) NOT NULL DEFAULT '#' AFTER `DESKRIPSI`,
	ADD COLUMN `IDX` SMALLINT NULL DEFAULT 0 AFTER `CONFIG`,
	DROP COLUMN `JENIS`,
	ADD INDEX `IDX` (`IDX`),
	ADD INDEX `LEVEL` (`LEVEL`);
